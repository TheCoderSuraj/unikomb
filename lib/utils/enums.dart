// user role should be on order based on their power
enum UserRole { viewer, editor, admin, dev }

enum PostCategory { regular, spectrum, events }

enum PostOptions { copyUrl, edit, delete, publish }

enum PostShowPlace { home, scheduled, draft }
